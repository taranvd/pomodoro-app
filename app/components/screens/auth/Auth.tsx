import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const Auth = (props: Props) => {
  return (
    <View>
      <Text>Auth</Text>
    </View>
  );
};

export default Auth;
